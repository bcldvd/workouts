sudo: required

before_script:
  - npm install
  # - npm install gulp
  - npm install phantomjs
  - echo -e "Host github.com\n\tStrictHostKeyChecking no\n" >> ~/.ssh/config
  - bower install -f

script: gulp test

language: node_js
node_js:
    - "0.10"
